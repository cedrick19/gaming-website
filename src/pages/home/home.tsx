import React from "react";
import { Page, View } from "framework7-react";
import NavBar from "../../components/nav-bar/Navbar"; // Ensure the import name matches the export

const HomePage = () => <Page name="home">this is home</Page>;

export default HomePage;
